# macOS
.DS_Store

# Logs
*.log

# Dotenv
.env
.env.*
!.env.example

# Database dumps (the WordPress migration source; kept out of git)
*.sql
*.dump

# Archived WordPress site (moved out of the way, kept locally as a backup)
/archive/

# Node / PHP tooling lives under site/ — see site/.gitignore
node_modules

# Submission secrets (never committed; placed on the server outside www/)
pdc-secrets.php
