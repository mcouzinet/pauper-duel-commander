{
  "name": "pdc-site",
  "type": "module",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "predev": "node scripts/copy-banlist.mjs && node scripts/warm-scryfall-cache.mjs",
    "dev": "astro dev",
    "prebuild": "node scripts/copy-banlist.mjs && node scripts/warm-scryfall-cache.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "lint": "astro check && tsc --noEmit",
    "pretest": "node scripts/copy-banlist.mjs",
    "test": "composer install --quiet && vendor/bin/phpunit"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^5.8.0",
    "vanilla-cookieconsent": "^3.1.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "@tailwindcss/vite": "^4.1.0",
    "tailwindcss": "^4.1.0",
    "typescript": "^5.7.0"
  }
}
