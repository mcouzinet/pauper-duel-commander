{#
/**
 * Module M06 - Footer
 *
 * Variables:
 * - logo (image): Logo du site
 * - site_name (string): Nom du site
 * - description (textarea): Description du site
 * - copyright (string): Texte de copyright (optionnel)
 * - background_color (string): Couleur de fond (optionnel, défaut: bg-gray-800)
 * - social_links (repeater): Liens réseaux sociaux (optionnel)
 *   - icon_svg (textarea): Code SVG de l'icône
 *   - url (url): Lien vers le réseau social
 *   - label (string): Label du lien (pour l'accessibilité)
 */
#}

<footer class="py-12 {{ module.background_color|default('bg-gray-800') }}">
    <div class="container mx-auto px-4">
        <div class="text-center max-w-2xl mx-auto">
            {% if module.logo %}
                <div class="mb-6">
                    <img src="{{ module.logo.url }}" alt="{{ module.logo.alt|default(module.site_name) }}" class="h-16 mx-auto">
                </div>
            {% elseif module.site_name %}
                <h3 class="text-2xl font-bold bg-gradient-gold bg-clip-text text-transparent mb-6">
                    {{ module.site_name }}
                </h3>
            {% endif %}

            {% if module.description %}
                <p class="text-muted-foreground mb-8">
                    {{ module.description }}
                </p>
            {% endif %}

            {% if module.social_links %}
                <div class="flex justify-center gap-4 mb-8">
                    {% for link in module.social_links %}
                        <a href="{{ link.url }}"
                           aria-label="{{ link.label }}"
                           class="w-10 h-10 bg-magic-purple/20 border border-magic-purple/40 rounded-lg flex items-center justify-center text-magic-gold hover:bg-magic-purple/30 transition-all"
                           {% if link.url starts with 'http' %}target="_blank" rel="noopener noreferrer"{% endif %}>
                            {{ link.icon_svg|raw }}
                        </a>
                    {% endfor %}
                </div>
            {% endif %}

            <div class="border-t border-magic-purple/20 pt-8">
                <p class="text-muted-foreground text-sm">
                    {{ module.copyright|default('© ' ~ "now"|date("Y") ~ ' ' ~ module.site_name ~ '. All rights reserved.') }}
                </p>
            </div>
        </div>
    </div>
</footer>
