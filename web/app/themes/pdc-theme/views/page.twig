{% extends "layouts/base.twig" %}

{% block content %}
    {# Render Gutenberg blocks (ACF Blocks) #}
    {{ post.content }}

    {# Legacy: Flexible Content modules (for old pages) #}
    {% if modules %}
        {% include 'modules/modules-loop.twig' %}
    {% endif %}
{% endblock %}
